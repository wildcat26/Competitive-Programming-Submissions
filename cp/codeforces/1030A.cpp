#include <iostream>
using namespace std;
int main () {
int n, ch;
scanf("%d", &n);
for (int i = 0; i < n; i++) {
	scanf(" %d", &ch);
	if (ch == 1)
		return 0*printf("HARD\n");
}
 return 0*printf("EASY\n");
}
