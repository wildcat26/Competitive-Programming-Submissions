#include <bits/stdc++.h>
using namespace std;
int n, m;

int pigCount(int i, int j) {
 
}

int main () {
 scanf("%d %d", &n, &m);
 char arr[n][m];
 for (int i = 0; i < n; i++)
 	for (int j = 0; j < m; j++)
 		scanf("%c", &arr[i][j]);
 return 0;
}
